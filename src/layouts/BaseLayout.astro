---
import SEOHead from '../components/SEOHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import '../styles/global.css';

interface BreadcrumbItem {
  name: string;
  url: string;
}

interface FAQItem {
  question: string;
  answer: string;
}

interface ProductData {
  productName: string;
  rating: number;
  reviewCount: number;
}

interface Props {
  title: string;
  description: string;
  image?: string;
  heroImage?: string;
  article?: boolean;
  pubDate?: string;
  updatedDate?: string;
  breadcrumbs?: BreadcrumbItem[];
  faqItems?: FAQItem[];
  productData?: ProductData;
  lang?: string;
  alternateUrl?: string;
}

const props = Astro.props;
const lang = props.lang || 'ja';
---

<!DOCTYPE html>
<html lang={lang}>
  <head>
    <SEOHead {...props} />
    {props.alternateUrl && lang === 'ja' && (
      <link rel="alternate" hreflang="en" href={props.alternateUrl} />
    )}
    {props.alternateUrl && lang === 'en' && (
      <link rel="alternate" hreflang="ja" href={props.alternateUrl} />
    )}
    <!-- Google AdSense -->
    <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-3397494907416647" crossorigin="anonymous" is:inline></script>
    <!-- Google Analytics -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-CRL4DMBVB2" is:inline></script>
    <script is:inline>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-CRL4DMBVB2');
    </script>
  </head>
  <body class="min-h-screen flex flex-col">
    <Header />
    <main class="flex-1">
      <slot />
    </main>
    <Footer />
  </body>
</html>
